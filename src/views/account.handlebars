<div
    style='display: flex; flex-direction: column; weight: 100vw;
height: 90vh; align-items: center; margin-top: 4rem'
>
    <h1>Your Account</h1>

    <h5>Name:</h5>
    <p>{{user.firstName}} {{user.lastName}}</p>
    <h5>Email:</h5>
    <p>{{user.email}}</p>
    <h5>Age:</h5>
    <p>{{user.age}}</p>
    <h5>Birth:</h5>
    <p>{{user.birth}}</p>
    <h5>Admin:</h5>
    <p>{{user.admin}}</p>
</div>